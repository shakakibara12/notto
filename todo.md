# TODO

* [ ] Open the current note in neovim.
  * [ ] Add a check if the file exists, don't overwrite it
    - This allows that we don't accidently delete our note.
  * [ ] Clean up chrono crate, minimize dependencies
  * [ ] Check if using protobuf have any use for us, or it's just another dependency.
